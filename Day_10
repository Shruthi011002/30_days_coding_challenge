
import math
import os
import random
import re
import sys



if __name__ == '__main__':
    n = int(input().strip())
    s= str(bin(n))
    a=0
    l=[]
    for i in s:
        if i=='1':
            a+=1
            l.append(a)
        else:
            a=0
    print(max(l))
